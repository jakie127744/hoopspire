node_modules/
dist/
.DS_Store
*.local
.env
.env.*
!.env.example

# Local CMS trash — deleted drafts, recoverable but not worth versioning
content/articles/.trash/

# Translation cache — regenerated by `npm run data`, and machine output we
# should not treat as source.
scripts/.cache/

# Raw API dumps and scratch harnesses kept while working on a scraper.
# Useful on disk, never in the repo: a snapshot of someone else's server,
# and a throwaway runner, neither of which is source.
/cba.json
/t.mjs
